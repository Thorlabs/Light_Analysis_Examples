[package]
name = "thorlabs-pm-scpi"
version = "1.0.0"
edition = "2021"
description = "Rust example: read a Thorlabs PMxxx power meter on Linux with plain SCPI commands over the kernel USBTMC driver"
publish = false

[dependencies]
nix = { version = "0.29", default-features = false, features = ["ioctl"] }
